const successCode = 200const errorCode = 500module.exports = {  success(json, msg){    let normal = {      code: successCode,      msg: msg || '操作成功'    }    for(var i in json){      normal[i] = json[i]    }    return normal  },  error(json, msg){    let normal = {      code: errorCode,      msg: msg || '操作失败'    }    for(var i in json){      normal[i] = json[i]    }    return normal  }}